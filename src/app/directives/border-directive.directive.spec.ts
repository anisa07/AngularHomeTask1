import { BorderDirective } from './border-directive.directive';

describe('BorderDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderDirective(this);
    expect(directive).toBeTruthy();
  });
});
